

let total = 0;

function addValue(value) {
    total += value;
    document.getElementById('totalDisplay').innerText = "Total: R$ " + total.toFixed(2);
}

{
    let total = 0;
    
    function addValue(value) {
        total += value;
        document.getElementById('totalDisplay').innerText = "Total: R$ " + total.toFixed(2);
    }}
